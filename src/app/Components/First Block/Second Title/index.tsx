import styles from "../../../Styles/firstBlock.module.scss";

export default function SecondTitle() {
    return (

            <div className={styles.firstBlock_secondTitle}>только лучше</div>

    );
}